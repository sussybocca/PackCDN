# Build settings
[build]
  command = "cd NETLIFY && npm install && npm run build"
  publish = "NETLIFY/out"

# Functions directory
[functions]
  directory = "NETLIFY/netlify/functions"

# Redirect all traffic to Next.js
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
