# Base directory – where commands run and publish are relative to
base = "NETLIFY"

# Build settings (no build needed for static HTML)
[build]
  command = "echo 'No build required'"
  publish = "."

# Remove functions if not used
# [functions]
#   directory = "netlify/functions"

# Optional: Redirect all traffic to index.html (for SPA routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
